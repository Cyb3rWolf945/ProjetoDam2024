<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BookShelf" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApp.MySplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_color</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.BookShelf</item>
    </style>

    <style name="Theme.BookShelf" parent="Base.Theme.BookShelf" />
</resources>